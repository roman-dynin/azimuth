services:

  db:
    image: postgres:18-alpine
    env_file:
      - docker/db.env
    ports:
      - "5432:5432"
    volumes:
      - ./docker/.data/db:/var/lib/postgresql
    restart: unless-stopped
